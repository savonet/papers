language: c
addons:
  apt:
    packages:
    - texlive
    - texlive-pictures
    - texlive-latex-extra
    - pgf
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y rubber

script: make
